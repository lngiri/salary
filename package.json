{
  "name": "salary-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "start": "cd backend && ([ -f dist/index.js ] || npm run build) && npm start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
