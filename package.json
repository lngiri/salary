{
  "name": "salary-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix backend --include=dev && npm install --prefix frontend --include=dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "start": "cd backend && npx prisma generate && npx prisma db push --accept-data-loss && npx prisma db seed && ([ -f dist/index.js ] || npm run build) && npm start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
