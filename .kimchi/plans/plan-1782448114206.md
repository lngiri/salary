I’ve explored the codebase. Good news: much of the backend is already done (`legalReference` field, migration, seed data, API endpoints). The `LegalInfoIcon` component also already exists. What’s missing is wiring it into the UI and adding the settings editor.

## Goal
Add legal-reference info (ⓘ) icons next to every salary head in the employee salary structure, payroll detail, and payslip views; plus a settings page to edit those references.

## Constraints
- No new dependencies (`react-tooltip` is already installed).
- Do not change the Prisma schema or backend controllers — they already support `legalReference`.
- Preserve existing UI patterns (Tailwind, existing component library).
- Do not fix unrelated frontend/backend type mismatches unless they block this feature.

## Chunks

### Chunk 1: Fix salary-head API path
**Scope:** `frontend/src/pages/EmployeeDetail.tsx:49-50`, `frontend/src/pages/Settings.tsx:32-33`
**Depends On:** None
**Accept When:**
- `GET /api/salary-heads` is called instead of `/api/config/salary-heads`.
- Network tab (or devtools) shows 200 for salary heads on Settings and EmployeeDetail pages.
**Open Questions:** None — route is confirmed mounted at `/api/salary-heads`.

### Chunk 2: Wire info icon into EmployeeDetail
**Scope:** `frontend/src/pages/EmployeeDetail.tsx` salary structure table
**Depends On:** Chunk 1
**Accept When:**
- Each salary head row in the table shows the ⓘ icon when `legalReference` is present.
- Hovering/clicking shows tooltip with Nepali text + disclaimer.
- No TypeScript errors.

### Chunk 3: Wire info icon into PayrollDetail
**Scope:** `frontend/src/pages/PayrollDetail.tsx`
**Depends On:** Chunk 1
**Accept When:**
- Backend already returns `transactions[].salaryHead.legalReference`.
- UI shows per-salary-head rows (current page only shows per-employee summary). We’ll add an expandable row or a detail view that lists each salary head with its icon.
- Tooltip renders correctly.

### Chunk 4: Create Payslip page and wire info icons
**Scope:** New file `frontend/src/pages/Payslip.tsx`, route in `App.tsx`
**Depends On:** Chunk 1
**Accept When:**
- `/payslip/:employeeId/:periodId` renders individual payslip.
- Earnings and deductions list each shows ⓘ icon next to the head name when `legalReference` exists.
- Navigation from PayrollDetail “Payslip” button works.

### Chunk 5: Add legal-reference editor in Settings
**Scope:** `frontend/src/pages/Settings.tsx`
**Depends On:** Chunk 1
**Accept When:**
- Settings salary-heads table has an Edit button per row.
- Inline edit or modal lets admin update `legalReference`.
- `PUT /api/salary-heads/:id` is called and the table refreshes.

### Chunk 6: Verify with build/typecheck
**Scope:** Full frontend
**Depends On:** Chunks 1–5
**Accept When:**
- `npm run build` in `frontend` completes with no errors.
- No `tsc` errors.

## Verification Strategy
- Manual browser check: open EmployeeDetail, PayrollDetail, Payslip, Settings and confirm icons + tooltips.
- `npm run build` in `frontend/`.
- Seed/re-seed backend and confirm salary heads have `legalReference`.

## Decision Log
- **Use existing `LegalInfoIcon.tsx`** — already implements tooltip + disclaimer; just needs integration.
- **Keep backend unchanged** — schema/controller/migration/seed already correct.
- **Create a dedicated Payslip page** — the backend endpoint exists but no frontend page renders it.
- **Fix `/config/salary-heads` → `/salary-heads`** — current path is broken; required for the feature to fetch data.

## Risks
- **Frontend/backend type mismatch** — `PayrollDetail` currently expects a flat transaction shape different from the backend. We may need a small adapter to display per-head details without a full refactor. Likelihood: high; mitigation: keep changes minimal.
- **Missing `/api/config/fiscal-years`** — Settings and PayrollRun already rely on this missing endpoint. Out of scope for this feature, but it may prevent fully testing Settings. We’ll only test the salary-heads section.
- **`react-tooltip` ID collisions** — `LegalInfoIcon` uses `Math.random()` for tooltip IDs. Acceptable for now, but could be improved later with stable IDs.

<!-- PLAN_COMPLETE -->

[38;5;244m✻ Worked for 1m 59s[0m